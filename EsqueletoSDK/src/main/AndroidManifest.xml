<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esqueleto.esqueletosdk">

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher">
        <provider
            android:name="com.esqueleto.esqueletosdk.provider.AppContentProvider"
            android:authorities="com.esqueleto.esqueletosdk.provider"
            android:syncable="true" />
    </application>

</manifest>
